<?xml version="1.0" encoding="utf-8"?>
<!--
    Place in: res/layout/activity_splash.xml

    Background is set to colorPrimary — must EXACTLY match
    the color in splash_background.xml so transition is seamless.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:background="@color/splash_bg">

    <!-- App logo — same drawable used in splash_background.xml -->
    <!-- Change @drawable/ic_logo to your actual logo drawable name -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/logo"
        android:contentDescription="App Logo" />

    <!-- App name below logo -->
    <TextView
        android:id="@+id/tvAppName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Smart Attendance"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="0.05" />

    <!-- Optional tagline -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Face-based attendance system"
        android:textColor="#CCFFFFFF"
        android:textSize="13sp" />

</LinearLayout>
